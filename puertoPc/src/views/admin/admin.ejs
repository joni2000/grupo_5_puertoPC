<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="https://pro.fontawesome.com/releases/v5.10.0/css/all.css" integrity="sha384-AYmEC3Yw5cVb3ZcuHtOA93w35dYTsvhLPVnYs9eStHfGJvOvKxVfELGroGkvsg+p" crossorigin="anonymous"/>
        <title><%= title %> </title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        <link rel="stylesheet" href="/css/style.css">
    </head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
          <div class="container-fluid">
            <a class="navbar-brand" href="#">Dashboard</a>
          </div>
    </nav>
    <main>
        <div class="container container-search">
            <div class="search-bar col-6 search-bar-admin">
                <input type="text" placeholder="Buscar productos">
                <form class="icon-search search-admin" action="">
                    <button type="submit">
                        <i class="fas fa-search"></i>
                    </button>
                </form>
            </div>
            <div>
                <a href="/admin/crear" class="btn btn-up btn-success">Cargar uno</a>
            </div>
        </div>
        <div class="container container-table">
            <table class="table">
            <thead class="table-dark col-12">
              <tr>
                  <th class="id">Id</th>
                  <th class="col-4">Nombre</th>
                  <th class="col-2">Categoría</th>
                  <th>Stock</th>
                  <th>Precio</th>
                  <th></th>
              </tr>
            </thead>
            <tbody class="table-products">
                <% products.forEach(product => { %>
                    <tr>
                        <th class="number-id"><%=product.id%></th>
                        <th><%=product.name%></th>
                        <th><%=product.category%></th>
                        <th><%=product.stock%></th>
                        <th><%=product.price%></th>
                        <th>
                            <a class="btn btn-success" href="/admin/editar/<%= product.id %>">
                              editar  
                            </a>
                            <form action="/admin/eliminar/<%- product.id %>?_method=DELETE" method="POST">
                              <button type="submit" class="btn btn-danger" >
                                eliminar
                              </button>
                            </form>
                        </th>
                    </tr>
                <% }) %>
            </tbody>
          </table>

        </div>
    </main>
</body>
</html>