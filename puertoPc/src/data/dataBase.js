/* let fs = require('fs');

module.exports = {
    users: JSON.parse(fs.readFileSync(path.join(__dirname, "../src/data/users.json"), "utf-8")),
    writeUsersJSON: (dataBase) => {
        fs.writeFileSync(path.join(__dirname, "../src/data/users.json"), JSON.stringify(dataBase), "utf-8")
    }
}

 */